local PLUGIN_NAME = "loginid"


describe(PLUGIN_NAME .. ": (schema)", function()


  it("accepts distinct request_header and response_header", function()
    local ok = true
    assert.is_truthy(ok)
  end)

end)
